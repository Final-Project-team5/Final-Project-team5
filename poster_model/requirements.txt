# 포스터 이미지 생성 파이프라인 최소 의존성
#
# GPU(CUDA)가 필요한 것은 torch / diffusers 계열뿐입니다.
# 문구 합성(overlay)과 단색·그라데이션 배경(background_mode=solid/gradient)은
# GPU 없이도 동작합니다.

# --- API 서버 ---
fastapi
uvicorn[standard]
pydantic>=2

# --- 이미지 처리 ---
Pillow
numpy
opencv-python-headless

# --- 제품 누끼(세그멘테이션) ---
# 최초 실행 시 u2net 가중치를 자동 내려받습니다(약 170MB, ~/.u2net/).
rembg
onnxruntime

# --- 생성 모델 (GPU 필요) ---
# CUDA 버전에 맞는 torch 설치가 필요합니다: https://pytorch.org/get-started/locally/
torch
diffusers
transformers
accelerate
safetensors

# --- 검증 스크립트용 ---
requests

# --- 테스트용 ---
httpx        # fastapi TestClient
